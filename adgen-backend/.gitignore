# secrets — never commit
.env

# python
.venv/
__pycache__/
*.pyc

# generated media (test outputs)
*.mp3
*.wav
*.mp4

# generated outputs (2-folder structure) + legacy job dirs
outputs/
jobs/

# avatar reference images (personal — keep local)
assets/
