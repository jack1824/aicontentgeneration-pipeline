================================================================
MODELS.txt — every model the platform uses, in one place
(companion to POD-REINSTALL-TOMORROW.txt; all open-source)
================================================================

Download base (HuggingFace, use /resolve/ never /blob/):
BASE = https://huggingface.co/Comfy-Org/Wan_2.2_ComfyUI_Repackaged/resolve/main/split_files


1) VIDEO GENERATION — Wan 2.2 family (Apache-2.0)
   ComfyUI folder: models/diffusion_models/
   ----------------------------------------------------------------
   wan2.2_t2v_high_noise_14B_fp8_scaled.safetensors   ~14 GB   B-roll (text-to-video), 1st half
   wan2.2_t2v_low_noise_14B_fp8_scaled.safetensors    ~14 GB   B-roll (text-to-video), 2nd half
   wan2.2_i2v_high_noise_14B_fp8_scaled.safetensors   ~14 GB   Product (image-to-video), 1st half
   wan2.2_i2v_low_noise_14B_fp8_scaled.safetensors    ~14 GB   Product (image-to-video), 2nd half
   wan2.2_s2v_14B_fp8_scaled.safetensors              ~14 GB   Avatar (sound-to-video lipsync)

   Remember it like this: t2v = B-roll ads, i2v = Product ads,
   s2v = Avatar ads. t2v/i2v come in PAIRS (high+low noise);
   s2v is a single file.

2) SHARED SUPPORT MODELS (all Wan pipelines need these)
   ----------------------------------------------------------------
   models/text_encoders/  umt5_xxl_fp8_e4m3fn_scaled.safetensors   ~6.5 GB  reads your prompts
   models/vae/            wan_2.1_vae.safetensors                  ~1.4 GB  turns latents into pixels
   models/audio_encoders/ wav2vec2_large_english_fp16.safetensors  ~0.6 GB  s2v ONLY — hears the voice,
                                                                            drives the mouth (ENGLISH-only:
                                                                            this is the Hindi lipsync gap)

3) SPEED LoRAs — the FAST/Preview preset (4-step Lightning)
   ComfyUI folder: models/loras/     (~0.6 GB each)
   ----------------------------------------------------------------
   wan2.2_t2v_lightx2v_4steps_lora_v1.1_high_noise.safetensors    B-roll fast mode
   wan2.2_t2v_lightx2v_4steps_lora_v1.1_low_noise.safetensors
   wan2.2_i2v_lightx2v_4steps_lora_v1_high_noise.safetensors      Product fast mode
   wan2.2_i2v_lightx2v_4steps_lora_v1_low_noise.safetensors

   Rule of thumb: LoRA ON = Preview drafts (minutes).
   LoRA OFF + 20 steps = Master quality (Avatar quality REQUIRES
   LoRA off — it visibly degrades s2v).

4) POST-PROCESSING — the ✨ Enhance chain (CodeFormer → SeedVR2 → RIFE)
   ----------------------------------------------------------------
   models/facerestore_models/ codeformer.pth                ~0.4 GB  face restore (manual wget:
     https://github.com/sczhou/CodeFormer/releases/download/v0.1.0/codeformer.pth)
   SeedVR2: seedvr2_ema_3b_fp8_e4m3fn.safetensors + ema_vae_fp16    upscaler — AUTO-downloads on
                                                                    first run (via the node pack)
   RIFE: rife49.pth                                                 2x smooth motion — AUTO-downloads
                                                                    on first run (lives inside the node)

5) CUSTOM NODE PACKS (ComfyUI Manager, not model files)
   ----------------------------------------------------------------
   [ ] SeedVR2 Video Upscaler (numz)
   [ ] Facerestore CF (Code Former)
   [ ] switch/math pack (pre-flight will confirm if missing)

6) OUTSIDE THE POD (APIs, no downloads)
   ----------------------------------------------------------------
   ElevenLabs eleven_multilingual_v2 ... every voice (EN + Hindi TTS)
   Gemini gemini-2.5-flash ............ the planning brain (proposals only)

----------------------------------------------------------------
TOTALS: ~85-90 GB on disk -> the 250 GB network volume fits
everything with room for outputs and future pipelines (LTX etc.)
================================================================

POD DISK LAYOUT (learned the hard way, 2026-07-05; updated after the
2026-07-06 forced migration to pod ls5k9rn9wxassp)
   App:   /ComfyUI  (container disk — RESET on every stop/restart/resize;
          rebuilt from image; Manager-installed node packs die with it)
   Store: /workspace/ComfyUI/models  (pod volume, grown to 400 GB — PERSISTS
          across stop/start and survives RunPod auto-migration; DELETED on
          Terminate)
   Wiring: /ComfyUI/extra_model_paths.yaml maps the store into ComfyUI.
   AFTER ANY POD RESET run:  /workspace/restore_after_reset.sh
   (copies wav2vec2 + codeformer.pth container-side, re-clones SeedVR2 +
   facerestore_cf Enhance packs; RIFE/VHS ship in the image) then restart
   ComfyUI (POST /manager/reboot — a 502 reply means it worked).
   NEVER `mv` between container and volume — a quota failure mid-move DELETED
   4 source files (ltx checkpoint, gemma, kijai-umt5, wav2vec2). Copy, verify,
   then delete. Preflight (Claude) diffs /models/* against all workflow JSONs
   before any render — the migration proved its worth: 9 files (i2v fp8 pair,
   6 LoRAs, LTX spatial upscaler) turned out container-side and had to be
   re-downloaded to the volume store.

7) NEXT PIPELINES (updated 2026-07-05 after web verification)
   ----------------------------------------------------------------

   A. CINEMATIC (LTX-2.3) .... INSTALLED + WIRED ("cinematic" mode)
      Native ComfyUI support (no custom nodes). Generates video AND
      its own synchronized audio (ambience/SFX) in one pass — prompts
      end with a SOUNDSCAPE sentence (planner does this automatically).
      workflow: workflows/ltx2_av.json (authored from the official
      template video_ltx2_3_t2v.json, prompt-enhancer branch removed).
      models/checkpoints/           ltx-2.3-22b-dev-fp8.safetensors      ~22 GB
      models/text_encoders/         gemma_3_12B_it_fp4_mixed.safetensors ~8 GB
      models/latent_upscale_models/ ltx-2.3-spatial-upscaler-x2-1.1.safetensors
      models/loras/                 ltx_2.3_22b_distilled_1.1_lora_dynamic_
                                    fro09_avg_rank_111_bf16.safetensors  (@0.5)
      ~5s @ 25fps per shot; two-stage render (half-res base -> 2x latent
      upscale -> refine). Narration script ducks the native audio under
      the ElevenLabs voice; no script = pure native soundtrack.

   B. LONG AVATAR (LongCat-Video-Avatar 1.5) .... WIRED, awaiting models
      Route: the Kijai WanVideoWrapper template (ALREADY on the pod — no
      node installs). workflow: workflows/longcat_avatar.json (converted
      from the pod's own template; MelBandRoFormer vocal separation
      BYPASSED — our TTS narration is already clean voice).
      ~15.8s continuous take (3x 93-frame windows, 13-frame overlap @16fps),
      lips driven by the uploaded narration; identity-stable. "longcat" mode.
      DOWNLOADS (pod terminal, 3 files ~19.6 GB):
      models/diffusion_models/LongCat/
        LongCat-Avatar-single_fp8_e4m3fn_scaled_mixed_KJ.safetensors  16.9 GB
        (Kijai/LongCat-Video_comfy -> Avatar/)
      models/loras/
        LongCat_distill_lora_alpha64_bf16.safetensors                  1.3 GB
      models/vae/
        Wan2_1_VAE_bf16.safetensors (Kijai/WanVideo_comfy)             1.4 GB
      models/text_encoders/
        umt5-xxl-enc-fp8_e4m3fn.safetensors (Kijai/WanVideo_comfy)     6.7 GB
        (Kijai's cached text encoder REJECTS the Comfy-Org fp8-SCALED
         umt5 — it needs Kijai's non-scaled fp8 conversion.)
      (wav2vec2-chinese speech encoder auto-downloads on first run.)

   PARKED: MultiTalk (Kijai WanVideoWrapper route) — revisit only if
   LongCat's multi-speaker quality disappoints.
   DROPPED: multilingual wav2vec2 swap for Wan-S2V — Hindi lip test
   passed to the user's satisfaction.

   Disk: ~90 GB (Wan era) + ~32 GB LTX set + ~22 GB LongCat ≈ 145 GB
   on the 250 GB network volume — fits.
================================================================
